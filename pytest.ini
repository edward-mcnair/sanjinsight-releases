[pytest]
# Use importlib import mode so pytest resolves modules from the project root
# rather than picking up stale copies from any git worktrees on sys.path.
addopts = --import-mode=importlib
testpaths = tests
